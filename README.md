# AmneziaWG Auto Installer для Alpine Linux

Автоматическая установка и настройка AmneziaWG VPN сервера на Alpine Linux с полной поддержкой обфускации трафика.

## 📋 Содержание

- [Особенности](#особенности)
- [Требования](#требования)
- [Быстрый старт](#быстрый-старт)
- [Детальная установка](#детальная-установка)
- [Управление клиентами](#управление-клиентами)
- [Устранение неполадок](#устранение-неполадок)
- [Архитектура](#архитектура)
- [FAQ](#faq)
- [Лицензия](#лицензия)

---

## ✨ Особенности

- 🚀 **Полностью автоматическая установка** - один скрипт для полной настройки
- 🔒 **AmneziaWG с обфускацией** - защита от DPI и блокировок
- 🎯 **Оптимизация сети** - предустановленные параметры для высокой производительности
- 🔧 **Простое управление клиентами** - добавление, удаление, просмотр статуса
- 📱 **QR-коды** - автоматическая генерация для мобильных устройств
- 🐧 **Нативная поддержка Alpine** - OpenRC, ash, apk
- 🌐 **Автоопределение IP** - не нужно вводить внешний адрес вручную
- 🔥 **NAT и Firewall** - автоматическая настройка iptables

---

## 📦 Требования

### Система
- **ОС:** Alpine Linux 3.23+ (Extended Edition рекомендуется)
- **Архитектура:** x86_64
- **ОЗУ:** минимум 512 MB (рекомендуется 1 GB+)
- **Диск:** минимум 2 GB свободного места
- **Ядро:** LTS, virt или edge (с соответствующими заголовками)

### Сеть
- Публичный IPv4 адрес
- Открытый UDP порт **36058** (или любой другой по вашему выбору)
- Доступ в интернет для скачивания пакетов

### Права
- Root доступ

---

## 🚀 Быстрый старт

```bash
# 1. Скачайте скрипты
wget https://raw.githubusercontent.com/your-repo/install_amneziawg.sh
wget https://raw.githubusercontent.com/your-repo/initawg_client.sh

# 2. Сделайте исполняемыми
chmod +x install_amneziawg.sh initawg_client.sh

# 3. Установите сервер
./install_amneziawg.sh

# 4. Установите менеджер клиентов
./initawg_client.sh

# 5. Создайте первого клиента
awg-client add alice
```

Готово! Ваш VPN сервер работает. Скопируйте конфигурацию или отсканируйте QR-код.

---

## 📖 Детальная установка

### Шаг 1: Подготовка Alpine Linux

#### Установка Alpine (если ещё не установлен)

1. **Скачайте ISO:**
   ```bash
   wget https://dl-cdn.alpinelinux.org/alpine/v3.23/releases/x86_64/alpine-extended-3.23.2-x86_64.iso
   ```

2. **Загрузитесь с ISO** и войдите как `root` (без пароля)

3. **Запустите установку:**
   ```bash
   setup-alpine
   ```
   
   Рекомендуемые настройки:
   - Клавиатура: `us` или `ru`
   - Hostname: `vpn-server` (любое)
   - Сеть: `eth0`, DHCP или статический IP
   - Пароль root: установите сильный пароль
   - Timezone: `UTC` или ваш часовой пояс
   - Proxy: `none`
   - NTP: `chrony`
   - SSH: `openssh`
   - Диск: выберите диск, `sys` режим, подтвердите

4. **Перезагрузитесь:**
   ```bash
   reboot
   ```

5. **Обновите систему:**
   ```bash
   apk update && apk upgrade
   ```

### Шаг 2: Установка AmneziaWG

#### Опция A: Скачать скрипты

```bash
# Создайте рабочую директорию
mkdir -p ~/amnezia-install
cd ~/amnezia-install

# Скачайте скрипты
wget https://raw.githubusercontent.com/your-repo/install_amneziawg.sh
wget https://raw.githubusercontent.com/your-repo/initawg_client.sh

# Права на выполнение
chmod +x *.sh
```

#### Опция B: Создать скрипты вручную

Скопируйте содержимое `install_amneziawg.sh` и `initawg_client.sh` в соответствующие файлы.

#### Запуск установки

```bash
./install_amneziawg.sh
```

**Что делает скрипт:**

1. ✅ Настраивает DNS (8.8.8.8, 1.1.1.1)
2. ✅ Определяет тип ядра и устанавливает заголовки
3. ✅ Обновляет систему и устанавливает зависимости
4. ✅ Оптимизирует сетевые параметры (sysctl, limits)
5. ✅ Компилирует и загружает модуль AmneziaWG
6. ✅ Устанавливает утилиты `awg` и `awg-quick`
7. ✅ Автоматически определяет внешний IP
8. ✅ Генерирует ключи сервера
9. ✅ Создаёт конфигурацию с параметрами обфускации
10. ✅ Настраивает OpenRC сервис
11. ✅ Настраивает iptables и NAT
12. ✅ Запускает сервис

**Время установки:** 2-5 минут (зависит от скорости интернета и CPU)

#### Проверка установки

```bash
# Проверка модуля ядра
lsmod | grep amneziawg

# Проверка сервиса
rc-service amnezia-awg status

# Проверка интерфейса
awg show wg0

# Проверка сети
ip addr show wg0
```

**Ожидаемый вывод:**
```
interface: wg0
  public key: <ваш_публичный_ключ>
  private key: (hidden)
  listening port: 36058
```

### Шаг 3: Установка менеджера клиентов

```bash
./initawg_client.sh
```

Это создаст утилиту `/usr/local/bin/awg-client` для управления клиентами.

---

## 👥 Управление клиентами

### Основные команды

```bash
# Добавить клиента
awg-client add <имя>

# Список всех клиентов
awg-client list

# Показать конфигурацию клиента
awg-client show <имя>

# Показать QR-код
awg-client qr <имя>

# Удалить клиента
awg-client remove <имя>
```

### Примеры использования

#### Добавление клиента

```bash
awg-client add alice
```

**Вывод:**
```
========================================
Создание клиента: alice
========================================
Генерация ключей...
Добавление в серверный конфиг...
Создание клиентского конфига...
Генерация QR-кода...
Перезагрузка сервиса...

========================================
Клиент 'alice' успешно создан!
========================================
IP адрес: 10.8.1.2
Конфиг: /etc/amnezia/awg/clients/alice/alice.conf
QR-код: /etc/amnezia/awg/clients/alice/alice-qr.png

========================================
Конфигурация для клиента:
========================================
[Interface]
PrivateKey = <приватный_ключ_клиента>
Address = 10.8.1.2/32
DNS = 1.1.1.1, 8.8.8.8
Jc = 5
Jmin = 10
Jmax = 50
S1 = 73
S2 = 32
H1 = 29823259
H2 = 2105934488
H3 = 1834425522
H4 = 20681889

[Peer]
PublicKey = <публичный_ключ_сервера>
PresharedKey = <preshared_ключ>
Endpoint = <ваш_IP>:36058
AllowedIPs = 0.0.0.0/0, ::/0
PersistentKeepalive = 25
========================================

[QR-код отображается здесь]
```

#### Список клиентов

```bash
awg-client list
```

**Вывод:**
```
========================================
Список клиентов AmneziaWG
========================================

ИМЯ                 IP АДРЕС        ДАТА         СТАТУС    
----------------------------------------------------------------
alice               10.8.1.2        2025-12-27   ONLINE
bob                 10.8.1.3        2025-12-27   OFFLINE
charlie             10.8.1.4        2025-12-26   ONLINE

Всего клиентов: 3
```

#### Показать QR-код

```bash
awg-client qr alice
```

Отобразит QR-код в терминале для сканирования мобильным приложением AmneziaWG.

#### Удаление клиента

```bash
awg-client remove bob
```

**Запрос подтверждения:**
```
Удалить клиента 'bob'? (yes/no): yes
Клиент 'bob' удалён
Резервная копия сохранена в: /etc/amnezia/awg/clients/.deleted
```

### Подключение клиентов

#### Мобильные устройства (Android/iOS)

1. Установите приложение AmneziaWG:
   - [Android](https://play.google.com/store/apps/details?id=org.amnezia.awg)
   - [iOS](https://apps.apple.com/app/amneziawg/id6478917276)

2. Получите QR-код:
   ```bash
   awg-client qr alice
   ```

3. Отсканируйте QR-код в приложении

4. Подключитесь!

#### Desktop (Windows/macOS/Linux)

1. Скачайте конфигурацию:
   ```bash
   cat /etc/amnezia/awg/clients/alice/alice.conf
   ```

2. Скопируйте содержимое в файл `alice.conf` на компьютере

3. Импортируйте в клиент AmneziaWG:
   - [Windows](https://github.com/amnezia-vpn/desktop-client/releases)
   - [macOS](https://github.com/amnezia-vpn/desktop-client/releases)
   - Linux: используйте `awg-quick up alice.conf`

---

## 🔧 Управление сервером

### OpenRC команды

```bash
# Запуск
rc-service amnezia-awg start

# Остановка
rc-service amnezia-awg stop

# Перезапуск
rc-service amnezia-awg restart

# Перезагрузка конфигурации (без разрыва соединений)
rc-service amnezia-awg reload

# Статус
rc-service amnezia-awg status
```

### Проверка состояния

```bash
# Просмотр подключений
awg show wg0

# Детальная статистика
awg show wg0 dump

# Последние handshake'и
awg show wg0 latest-handshakes

# Трансфер данных
awg show wg0 transfer
```

### Логи

```bash
# Логи установки
cat /var/log/amneziawg_install.log

# Логи OpenRC
rc-service amnezia-awg status

# Системные логи
dmesg | grep amneziawg
```

### Файлы конфигурации

```
/etc/amnezia/awg/
├── wg0.conf                    # Конфигурация сервера
├── server_private.key          # Приватный ключ сервера
├── server_public.key           # Публичный ключ сервера
├── server_ip.txt               # IP адрес сервера
└── clients/                    # Директория клиентов
    ├── alice/
    │   ├── private.key
    │   ├── public.key
    │   ├── preshared.key
    │   ├── ip.txt
    │   ├── created.txt
    │   ├── alice.conf          # Конфиг для клиента
    │   ├── qr.txt              # QR-код (ASCII)
    │   └── alice-qr.png        # QR-код (PNG)
    └── .deleted/               # Резервные копии удалённых
```

---

## 🛠 Устранение неполадок

### Модуль не загружается

**Проблема:** `lsmod | grep amneziawg` ничего не показывает

**Решения:**

1. Проверьте заголовки ядра:
   ```bash
   uname -r
   apk list | grep linux-headers
   ```

2. Установите правильные заголовки:
   ```bash
   # Для LTS ядра
   apk add linux-lts-dev
   
   # Для virt ядра
   apk add linux-virt-dev
   ```

3. Перезагрузите систему:
   ```bash
   reboot
   ```

4. Попробуйте загрузить вручную:
   ```bash
   modprobe amneziawg
   dmesg | tail
   ```

### Сервис не запускается

**Проблема:** `rc-service amnezia-awg start` выдаёт ошибку

**Решения:**

1. Проверьте конфигурацию:
   ```bash
   awg-quick strip /etc/amnezia/awg/wg0.conf
   ```

2. Проверьте права на файлы:
   ```bash
   ls -la /etc/amnezia/awg/
   chmod 600 /etc/amnezia/awg/wg0.conf
   chmod 600 /etc/amnezia/awg/server_private.key
   ```

3. Запустите вручную для диагностики:
   ```bash
   awg-quick up /etc/amnezia/awg/wg0.conf
   ```

### Клиент не подключается

**Проблема:** Клиент не может установить соединение

**Проверки:**

1. Убедитесь, что порт открыт:
   ```bash
   # На сервере
   netstat -ulnp | grep 36058
   
   # С другого компьютера
   nc -zvu <server_ip> 36058
   ```

2. Проверьте firewall провайдера (облачный firewall, security groups)

3. Проверьте iptables:
   ```bash
   iptables -L -n -v | grep wg0
   iptables -t nat -L -n -v
   ```

4. Проверьте handshake на сервере:
   ```bash
   awg show wg0 latest-handshakes
   ```

5. Проверьте IP forwarding:
   ```bash
   sysctl net.ipv4.ip_forward
   # Должно быть: net.ipv4.ip_forward = 1
   ```

### Медленная скорость

**Решения:**

1. Проверьте MTU:
   ```bash
   # На сервере
   ip link show wg0
   
   # В клиентском конфиге добавьте:
   # MTU = 1420
   ```

2. Измените алгоритм TCP congestion:
   ```bash
   # Попробуйте bbr вместо hybla
   sysctl -w net.ipv4.tcp_congestion_control=bbr
   ```

3. Проверьте нагрузку на CPU:
   ```bash
   top
   ```

### DNS не работает

**Проблема:** Интернет работает, но сайты не открываются

**Решения:**

1. Проверьте DNS в клиентском конфиге:
   ```ini
   [Interface]
   DNS = 1.1.1.1, 8.8.8.8
   ```

2. На Android проверьте настройки "Private DNS"

3. Используйте альтернативные DNS:
   ```ini
   DNS = 9.9.9.9, 149.112.112.112  # Quad9
   DNS = 208.67.222.222, 208.67.220.220  # OpenDNS
   ```

---

## 🏗 Архитектура

### Параметры обфускации

AmneziaWG использует следующие параметры для обхода DPI:

```ini
Jc = 5          # Junk packet count
Jmin = 10       # Junk packet minimum size
Jmax = 50       # Junk packet maximum size
S1 = 73         # Init packet junk size
S2 = 32         # Response packet junk size
H1 = 29823259   # Init packet magic header
H2 = 2105934488 # Response packet magic header
H3 = 1834425522 # Underload packet magic header
H4 = 20681889   # Transport packet magic header
```

Эти параметры добавляют случайные данные и изменяют заголовки пакетов, делая трафик похожим на обычный HTTPS.

### Сетевая топология

```
Internet
   │
   ├── Firewall (UDP 36058)
   │
   └── Alpine Server (eth0: public IP)
        │
        └── wg0 (10.8.1.1/24)
             │
             ├── Client 1 (10.8.1.2)
             ├── Client 2 (10.8.1.3)
             └── Client N (10.8.1.x)
```

### Оптимизации sysctl

Скрипт применяет следующие оптимизации:

- **TCP параметры:** Hybla congestion control для высокой latency
- **Буферы:** Увеличенные rmem/wmem для высокой пропускной способности
- **Backlog:** Увеличенные очереди для обработки burst трафика
- **IP forwarding:** Включен для роутинга VPN трафика

### Firewall правила

```bash
# Разрешить VPN трафик
iptables -A FORWARD -i wg0 -j ACCEPT
iptables -A FORWARD -o wg0 -j ACCEPT

# NAT для выхода в интернет
iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
```

---

## ❓ FAQ

### В чём разница между AmneziaWG и обычным WireGuard?

AmneziaWG - это форк WireGuard с добавлением обфускации трафика. Он помогает обходить DPI (Deep Packet Inspection) и блокировки провайдеров, делая VPN трафик похожим на обычный HTTPS.

### Можно ли использовать стандартный WireGuard клиент?

Нет. Требуется клиент AmneziaWG, так как используются кастомные параметры обфускации (Jc, Jmin, H1-H4, etc).

### Как изменить порт?

1. Отредактируйте `/etc/amnezia/awg/wg0.conf`:
   ```ini
   ListenPort = 51820  # новый порт
   ```

2. Перезапустите сервис:
   ```bash
   rc-service amnezia-awg restart
   ```

3. Обновите конфиги клиентов

### Как изменить сеть VPN?

По умолчанию используется `10.8.1.0/24`. Для изменения:

1. Отредактируйте `/etc/amnezia/awg/wg0.conf`:
   ```ini
   Address = 10.10.10.1/24
   ```

2. Обновите `VPN_NETWORK` в `/usr/local/bin/awg-client`:
   ```bash
   VPN_NETWORK="10.10.10"
   ```

3. Пересоздайте клиентов

### Сколько клиентов можно подключить?

Технически до 253 клиентов в одной подсети /24. На практике ограничение - пропускная способность сервера и CPU.

### Работает ли IPv6?

Текущая конфигурация использует только IPv4. Для IPv6 нужно:
- Добавить IPv6 адрес в `Address`
- Настроить IPv6 forwarding
- Добавить IPv6 firewall правила

### Как сделать бэкап?

```bash
# Бэкап конфигурации
tar -czf amnezia-backup-$(date +%Y%m%d).tar.gz /etc/amnezia/

# Восстановление
tar -xzf amnezia-backup-YYYYMMDD.tar.gz -C /
rc-service amnezia-awg restart
```

### Как обновить AmneziaWG?

```bash
cd /usr/src/amneziawg-linux-kernel-module
git pull
cd src
make && make install
modprobe -r amneziawg
modprobe amneziawg

cd /usr/src/amneziawg-tools
git pull
cd src
make WITH_WGQUICK=yes && make install WITH_WGQUICK=yes

rc-service amnezia-awg restart
```

### Поддерживается ли split-tunneling?

Да. В клиентском конфиге измените `AllowedIPs`:

```ini
# Весь трафик через VPN
AllowedIPs = 0.0.0.0/0, ::/0

# Только определённые сети
AllowedIPs = 10.8.1.0/24, 192.168.1.0/24

# Всё кроме локальной сети
AllowedIPs = 0.0.0.0/1, 128.0.0.0/1
```

---

## 📊 Мониторинг и статистика

### Простая статистика

```bash
# Подключённые клиенты
awg show wg0 | grep peer -A 5

# Трафик клиентов
awg show wg0 transfer

# Последняя активность
awg show wg0 latest-handshakes
```

### Создание скрипта мониторинга

```bash
cat > /usr/local/bin/awg-stats << 'EOF'
#!/bin/ash
printf "AmneziaWG Statistics\n"
printf "====================\n\n"
printf "Interface: wg0\n"
printf "Status: "
if ip link show wg0 >/dev/null 2>&1; then
    printf "UP\n"
else
    printf "DOWN\n"
fi
printf "\n"
awg show wg0
EOF

chmod +x /usr/local/bin/awg-stats
```

Использование: `awg-stats`

---

## 🔐 Безопасность

### Рекомендации

1. **Регулярно обновляйте систему:**
   ```bash
   apk update && apk upgrade
   ```

2. **Используйте сильные ключи** (генерируются автоматически)

3. **Ограничьте SSH доступ:**
   ```bash
   # Разрешить только с определённых IP
   iptables -A INPUT -p tcp --dport 22 -s <trusted_ip> -j ACCEPT
   iptables -A INPUT -p tcp --dport 22 -j DROP
   ```

4. **Мониторьте логи:**
   ```bash
   tail -f /var/log/messages
   ```

5. **Регулярно делайте бэкапы** конфигурации

### Hardening

```bash
# Отключите неиспользуемые сервисы
rc-update del <service>

# Настройте fail2ban (опционально)
apk add fail2ban
rc-update add fail2ban

# Ограничьте rate на VPN порту
iptables -A INPUT -p udp --dport 36058 -m limit --limit 100/s -j ACCEPT
```

---

## 🤝 Содействие

Если вы нашли баг или хотите предложить улучшение:

1. Откройте [Issue](https://github.com/your-repo/issues)
2. Создайте Pull Request
3. Свяжитесь с разработчиком

---

## 📄 Лицензия

Эти скрипты распространяются под лицензией MIT.

AmneziaWG - форк WireGuard, разработанный [Amnezia VPN](https://amnezia.org/).

---

## 🙏 Благодарности

- [Amnezia VPN](https://amnezia.org/) - за AmneziaWG
- [WireGuard](https://www.wireguard.com/) - за оригинальный протокол
- [Alpine Linux](https://alpinelinux.org/) - за минималистичный дистрибутив

---

## 📞 Поддержка

- **Документация AmneziaWG:** https://docs.amnezia.org/
- **GitHub AmneziaWG:** https://github.com/amnezia-vpn
- **Telegram:** @amnezia_vpn_en

---

**Дисклеймер:** Используйте VPN ответственно и в соответствии с законами вашей страны. Авторы скриптов не несут ответственности за использование программного обеспечения.
