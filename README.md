---

# Инструкция по использованию

debian подобный дистр



## 1. Установка сервера
```bash
# Скачать и запустить установочный скрипт
wget -O /root/install_amneziawg.sh https://your-server.com/install_amneziawg.sh
# или создать файл вручную из скрипта выше

# Запустить установку
bash /root/install_amneziawg.sh
```

Скрипт автоматически:
- Настроит DNS
- Обновит систему
- Оптимизирует сетевые параметры
- Установит AmneziaWG
- Определит внешний IP
- Создаст конфигурацию
- Настроит firewall
- Запустит сервис

## 2. Управление клиентами
```bash
# Добавить нового клиента
awg-client add alice

# Список всех клиентов
awg-client list

# Показать конфиг клиента
awg-client show alice

# Показать QR-код
awg-client qr alice

# Удалить клиента
awg-client remove alice

# Информация о сервере
awg-client info

# Экспорт всех конфигов
awg-client export
```

## 3. Управление сервисом
```bash
# Статус
systemctl status amnezia-awg

# Перезапуск
systemctl restart amnezia-awg

# Просмотр подключений
awg show wg0

# Логи
journalctl -u amnezia-awg -f
```

Всё полностью автономно и готово к использованию! 🚀
